// Same-origin (Railway hosts site + API): leave as "".
// Split deploy (Vercel/static frontend + Railway API): set Railway URL, e.g.:
// window.PORTAL_API_BASE = "https://your-app.up.railway.app/api";
window.PORTAL_API_BASE = window.PORTAL_API_BASE || "";
