{
  "name": "elgayarzeinlawfirm",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm --prefix portal-server start",
    "local": "npm --prefix portal-server run local",
    "railway": "npm --prefix portal-server run railway",
    "portal": "npm --prefix portal-server start",
    "portal:dev": "npm --prefix portal-server run dev",
    "portal:local": "npm --prefix portal-server run local",
    "portal:railway": "npm --prefix portal-server run railway",
    "site": "npx --yes serve -l tcp://0.0.0.0:3001",
    "postinstall": "node scripts/postinstall.js",
    "build": "node scripts/sync-deploy-config.js",
    "pages:config": "node scripts/sync-deploy-config.js",
    "pages:deploy": "wrangler pages deploy dist",
    "optimize:images": "py -3 scripts/optimize-site-images.py"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "firebase": "^12.13.0"
  }
}
